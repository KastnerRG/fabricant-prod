name: Deploy to Fabricant-Host

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:

    runs-on: self-hosted

    steps:
      - name: Run Ansible Playbook
        run: |
          pushd /home/fabricant-admin/fabricant-prod
          git pull origin main
          git submodule update --init --recursive
          source /home/fabricant-admin/fabricant-prod/.venv/bin/activate
          ansible-playbook playbook.yaml
          popd